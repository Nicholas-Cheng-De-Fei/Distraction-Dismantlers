import { Text, View, Image} from "react-native";
import { styles } from "../assets/style";

export default function Login() {
  return (
    <View style = {[styles.background, styles.centerContentContainer]}>
      <Text>Login Page</Text>
    </View>
  );
}